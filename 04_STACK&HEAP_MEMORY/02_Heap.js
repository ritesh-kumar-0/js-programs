/* Heap memory is a memory area used for dynamically allocated data,
 especially objects and other complex/reference values.*/
 let student = {    // student = Stack
    name: "Ritesh", // heap 
    age: 20
};
//The variable student is associated with a reference to the object stored in the heap.
console.log(student);